//WAP to print area of a square rectangle and square

#include <stdio.h>
#include<math.h>

int main(){
    int n;
    printf("Enter a NUber"
    );
    scanf("%d",n);
    for (int i = 2; i < sqrt(n); i++)
    {
        if(n%i ==0){
           
        }
    }
    printf("It s not a prime number");
    return 0;

}